// API Configuration
// With Vite proxy, all /api/* requests are automatically forwarded to the backend.
// Use empty string so fetch('/api/...') goes through the proxy server-side.
export const API_BASE_URL = '';
